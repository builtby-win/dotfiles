"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "./scripts/conductor-herdr-workspace.sh setup"
archive = "./scripts/conductor-herdr-workspace.sh archive"
run_mode = "concurrent"
