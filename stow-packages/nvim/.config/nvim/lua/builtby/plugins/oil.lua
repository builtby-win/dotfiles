require("oil").setup({
  default_file_explorer = false,
  columns = { "icon" },
  float = { border = "rounded" },
  skip_confirm_for_simple_edits = true,
  view_options = {
    show_hidden = true,
  },
})
