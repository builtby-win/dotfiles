# =============================================================================
# builtby.win tmux bootstrap (basic profile)
# =============================================================================

source-file -q "$HOME/.config/tmux/builtby/core.conf"
source-file -q "$HOME/.config/tmux/builtby/basic.conf"
source-file -q "$HOME/.tmux.local.conf"
