# =============================================================================
# builtby.win tmux bootstrap (pro profile)
# =============================================================================

source-file -q "$HOME/.config/tmux/builtby/core.conf"
source-file -q "$HOME/.config/tmux/builtby/pro.conf"
source-file -q "$HOME/.tmux.local.conf"
