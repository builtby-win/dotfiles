# =============================================================================
# builtby.win tmux pro profile
# Non-invasive: keeps tmux defaults and your existing keybinds.
# =============================================================================

# Intentionally no `bind`/`unbind` commands here.
# Add your personal remaps in ~/.tmux.local.conf.
