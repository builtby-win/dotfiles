# App export inventory

This directory stores native restore artifacts for macOS apps.

- `raycast/archive/` - versioned `.rayconfig` exports
- `rectangle-pro/` - Rectangle Pro JSON exports
- `bettertouchtool/` - BetterTouchTool preset exports

Use:

```bash
bb restore macos-apps
```

to reveal these files and print the import flow.
