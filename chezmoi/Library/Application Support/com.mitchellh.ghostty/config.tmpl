# builtby.win/dotfiles — thin wrapper; all config lives in the dotfiles repo.
# Created once by chezmoi. Ghostty reloads this on change and picks up the
# full config from the repo, so updates are live without chezmoi apply.
config-file = {{ env "DOTFILES_DIR" }}/chezmoi/Library/Application Support/com.mitchellh.ghostty/builtby-config
